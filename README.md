# 📱 BTEU Schedule (БТЭУ Расписание)

Мобильное приложение для просмотра расписания занятий, экзаменов и другой учебной информации Белорусского торгово-экономического университета (БТЭУ).

## 🎯 Описание

BTEU Schedule — это полнофункциональное приложение для студентов БТЭУ, предоставляющее удобный доступ к расписанию занятий, экзаменам, тестам и другой учебной информации. Приложение включает современный пользовательский интерфейс, интеллектуальный поиск, аналитику расписания и AI-ассистента.

## ✨ Основные возможности

### Для студентов:
- 📅 **Расписание занятий** — просмотр расписания по дням и неделям
- 📝 **Экзамены и тесты** — информация о предстоящих экзаменах и контрольных работах
- 🔍 **Умный поиск** — быстрый поиск по группам, предметам и преподавателям
- 📊 **Аналитика расписания** — статистика и анализ учебной нагрузки
- 🤖 **AI-ассистент** — помощь в вопросах, связанных с расписанием
- 🔔 **Уведомления** — напоминания о занятиях и важных событиях
- 🌐 **Многоязычность** — поддержка русского, белорусского и английского языков
- 🎨 **Темная тема** — комфортная работа в любое время суток
- 📱 **Виджеты** — быстрый доступ к расписанию с главного экрана

### Для администраторов:
- 📤 **Загрузка расписания** — автоматический парсинг Excel файлов
- 🔄 **Синхронизация** — обновление данных через FTP или локальные файлы
- 🗄️ **Управление данными** — удобное администрирование через REST API

## 🏗️ Архитектура проекта

Проект состоит из трех основных компонентов:

```
BTEU_Schedule/
├── android-app/          # Android приложение (Kotlin + Jetpack Compose)
├── flutter-app/          # Flutter клиент (в разработке)
├── backend/              # Python backend (Flask + PostgreSQL)
├── scripts/              # Вспомогательные скрипты
│   ├── powershell/       # PowerShell скрипты для автоматизации
│   └── python/           # Python скрипты и экспериментальный код
└── docs/                 # Документация и изображения
    └── images/
```

## 🛠️ Технологический стек

### Android приложение:
- **Язык:** Kotlin
- **UI Framework:** Jetpack Compose
- **Архитектура:** MVVM + Repository Pattern
- **Dependency Injection:** Hilt
- **База данных:** Room
- **Сеть:** Retrofit + OkHttp
- **Минимальная версия SDK:** Android 7.0 (API 24)
- **Целевая версия SDK:** Android 14 (API 34)

### Backend:
- **Framework:** Flask
- **База данных:** PostgreSQL
- **Парсинг:** openpyxl, xlrd, BeautifulSoup4
- **CORS:** flask-cors
- **AI:** OpenAI API / Google Gemini / Ollama (опционально)

### Инструменты разработки:
- **IDE:** Android Studio
- **Версия Kotlin:** 1.9+
- **Версия Python:** 3.8+

## 📋 Требования

### Для Android приложения:
- Android Studio Hedgehog или новее
- JDK 17 или новее
- Android SDK 34
- Gradle 8.0+

### Для Backend:
- Python 3.8 или новее
- PostgreSQL 12+
- pip или pipenv для управления зависимостями

## 🚀 Установка и запуск

### Android приложение

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/DanW11d/BTEU_Schedule.git
   cd BTEU_Schedule/android-app
   ```

2. **Откройте проект в Android Studio:**
   - File → Open → выберите папку `android-app`
   - Дождитесь синхронизации Gradle

3. **Настройте API endpoint:**
   - В файле `app/src/main/java/.../data/config/AppConfig.kt`
   - Установите `BASE_URL` на адрес вашего backend сервера

4. **Запустите приложение:**
   - Подключите устройство или запустите эмулятор
   - Нажмите Run (Shift+F10)

### Backend

1. **Установите зависимости:**
   ```bash
   cd backend
   pip install -r requirements.txt
   ```

2. **Настройте базу данных:**
   - Установите PostgreSQL
   - Создайте базу данных и пользователя
   - В файле `backend/.env` укажите параметры подключения:
     ```env
     DB_HOST=localhost
     DB_PORT=5432
     DB_NAME=bteu_schedule
     DB_USER=your_username
     DB_PASSWORD=your_password
     ```

3. **Запустите сервер:**
   ```bash
   python server.py
   ```
   
   Или используйте скрипты запуска:
   ```bash
   # Windows
   start_server.bat
   
   # Linux/Mac
   ./start_server.sh
   ```

4. **Проверьте работу API:**
   - Откройте в браузере: `http://localhost:5000/v1/health`
   - Должен вернуться статус: `{"status": "ok"}`

### Автоматический запуск сервера (Windows)

Для автоматического запуска backend при старте системы:

```powershell
cd backend
.\setup_autostart.ps1
```

## 📡 API Endpoints

Основные endpoints backend API:

- `GET /v1/faculties` — список факультетов
- `GET /v1/groups` — список групп
- `GET /v1/schedule/group/<code>/week` — расписание на неделю
- `GET /v1/schedule/group/<code>/day/<day>` — расписание на день
- `GET /v1/exams/group/<code>` — экзамены группы
- `GET /v1/tests/group/<code>` — тесты группы
- `GET /v1/analytics/group/<code>` — аналитика расписания
- `POST /v1/ai/chat` — AI чат
- `POST /v1/search` — поиск
- `GET /v1/bell-schedule` — расписание звонков

Полная документация API доступна после запуска сервера по адресу: `http://localhost:5000/v1/`

## 🔧 Настройка

### Переменные окружения (Backend)

Создайте файл `backend/.env`:

```env
# База данных
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bteu_schedule
DB_USER=your_username
DB_PASSWORD=your_password

# AI сервисы (опционально)
OPENAI_API_KEY=your_openai_key
GEMINI_API_KEY=your_gemini_key
OLLAMA_BASE_URL=http://localhost:11434

# Настройки сервера
FLASK_ENV=development
FLASK_DEBUG=True
PORT=5000
```

### Настройки Android приложения

- API Base URL: `app/src/main/java/.../data/config/AppConfig.kt`
- Поддержка языков: `app/src/main/res/values/strings.xml`
- Цветовая схема: `app/src/main/java/.../ui/theme/`

## 📝 Загрузка расписания

Для загрузки расписания в базу данных:

1. **Подготовьте Excel файлы** с расписанием (формат должен соответствовать парсеру)

2. **Используйте API endpoint:**
   ```bash
   curl -X POST http://localhost:5000/v1/admin/parse-excel \
     -F "file=@schedule.xlsx" \
     -F "group_code=ПО-31"
   ```

3. **Или используйте скрипты синхронизации:**
   ```bash
   python backend/sync_schedule.py
   python backend/download_schedule.py  # для загрузки с FTP
   ```

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие проекта! Пожалуйста:

1. Fork репозитория
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 👥 Авторы

- **Filchenko Daniil** ([@DanW11d](https://github.com/DanW11d))

## 🙏 Благодарности

- Белорусскому торгово-экономическому университету (БТЭУ)
- Всем студентам, принявшим участие в тестировании и разработке

## 📞 Контакты

По вопросам и предложениям создавайте Issues в репозитории.

---

⭐ Если проект был полезен, поставьте звезду!
